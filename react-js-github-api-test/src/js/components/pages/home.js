// @flow
import * as React from 'react';
import PageTemplate from '../common/pageTemplate';
import Users from '../users/Users';

const Home = () => (
  <PageTemplate>
    <Users />
  </PageTemplate>
);

export default Home;
